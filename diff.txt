diff --git a/libs/database/prisma/schema.prisma b/libs/database/prisma/schema.prisma
index a9f44e0..8b78274 100644
--- a/libs/database/prisma/schema.prisma
+++ b/libs/database/prisma/schema.prisma
@@ -3,8 +3,8 @@ generator client {
 }
 
 datasource db {
-  provider  = "postgresql"
-  url       = env("DATABASE_URL")
+  provider = "postgresql"
+  url      = env("DATABASE_URL")
 }
 
 // ─── Enums ──────────────────────────────────────────────
@@ -234,25 +234,25 @@ enum GrievanceRelatedType {
 }
 
 model Grievance {
-  id                String                @id @default(uuid()) @db.Uuid
-  institutionId     String                @map("institution_id") @db.Uuid
-  studentId         String?               @map("student_id") @db.Uuid
-  facultyId         String?               @map("faculty_id") @db.Uuid
-  source            GrievanceSource       @default(STUDENT)
-  category          GrievanceCategory
-  subject           String
-  description       String                @db.Text
-  relatedType       GrievanceRelatedType? @map("related_type")
-  relatedId         String?               @map("related_id") @db.Uuid
-  isAnonymous       Boolean               @default(false) @map("is_anonymous")
-  status            String                @default("OPEN")
-  priority          ServiceRequestPriority @default(MEDIUM)
-  createdAt         DateTime              @default(now()) @map("created_at")
-  updatedAt         DateTime              @updatedAt @map("updated_at")
-
-  institution       Institution @relation(fields: [institutionId], references: [id])
-  student           Student?    @relation(fields: [studentId], references: [id])
-  faculty           Faculty?    @relation(fields: [facultyId], references: [id])
+  id            String                 @id @default(uuid()) @db.Uuid
+  institutionId String                 @map("institution_id") @db.Uuid
+  studentId     String?                @map("student_id") @db.Uuid
+  facultyId     String?                @map("faculty_id") @db.Uuid
+  source        GrievanceSource        @default(STUDENT)
+  category      GrievanceCategory
+  subject       String
+  description   String                 @db.Text
+  relatedType   GrievanceRelatedType?  @map("related_type")
+  relatedId     String?                @map("related_id") @db.Uuid
+  isAnonymous   Boolean                @default(false) @map("is_anonymous")
+  status        String                 @default("OPEN")
+  priority      ServiceRequestPriority @default(MEDIUM)
+  createdAt     DateTime               @default(now()) @map("created_at")
+  updatedAt     DateTime               @updatedAt @map("updated_at")
+
+  institution Institution @relation(fields: [institutionId], references: [id])
+  student     Student?    @relation(fields: [studentId], references: [id])
+  faculty     Faculty?    @relation(fields: [facultyId], references: [id])
 
   @@index([institutionId])
   @@index([studentId])
@@ -353,51 +353,51 @@ model Institution {
   classLevels   ClassLevel[]
   batches       Batch[]
 
-  academicTerms          AcademicTerm[]
-  sections               Section[]
-  courses                Course[]
-  curriculums            Curriculum[]
-  curriculumTerms        CurriculumTerm[]
-  curriculumElectiveGroups CurriculumElectiveGroup[]
-  curriculumCourses      CurriculumCourse[]
-  coursePrerequisites    CoursePrerequisite[]
-  enrollments            Enrollment[]
-  courseAssignments      CourseAssignment[]
-  courseOfferings        CourseOffering[]
-  timetableEntries       TimetableEntry[]
-  attendanceSessions     AttendanceSession[]
-  attendanceRecords      AttendanceRecord[]
-  courseResources        CourseResource[]
-  assignments            Assignment[]
-  assignmentSubmissions  AssignmentSubmission[]
-  exams                  Exam[]
-  examCourses            ExamCourse[]
-  marks                  Mark[]
-  calendarEvents         CalendarEvent[]
-  announcements          Announcement[]
-  notifications          Notification[]
-  certificateRequests    CertificateRequest[]
-  grievances             Grievance[]
-  certificates           Certificate[]
-  studentDocuments       StudentDocument[]
-  feedbackForms          FeedbackForm[]
-  feedbackSubmissions    FeedbackSubmission[]
-  serviceRequests        ServiceRequest[]
-  clubs                  Club[]
-  clubMemberships        ClubMembership[]
-  clubEvents             ClubEvent[]
-  clubEventRegistrations ClubEventRegistration[]
-  studentSkills          StudentSkill[]
+  academicTerms             AcademicTerm[]
+  sections                  Section[]
+  courses                   Course[]
+  curriculums               Curriculum[]
+  curriculumTerms           CurriculumTerm[]
+  curriculumElectiveGroups  CurriculumElectiveGroup[]
+  curriculumCourses         CurriculumCourse[]
+  coursePrerequisites       CoursePrerequisite[]
+  enrollments               Enrollment[]
+  courseAssignments         CourseAssignment[]
+  courseOfferings           CourseOffering[]
+  timetableEntries          TimetableEntry[]
+  attendanceSessions        AttendanceSession[]
+  attendanceRecords         AttendanceRecord[]
+  courseResources           CourseResource[]
+  assignments               Assignment[]
+  assignmentSubmissions     AssignmentSubmission[]
+  exams                     Exam[]
+  examCourses               ExamCourse[]
+  marks                     Mark[]
+  calendarEvents            CalendarEvent[]
+  announcements             Announcement[]
+  notifications             Notification[]
+  certificateRequests       CertificateRequest[]
+  grievances                Grievance[]
+  certificates              Certificate[]
+  studentDocuments          StudentDocument[]
+  feedbackForms             FeedbackForm[]
+  feedbackSubmissions       FeedbackSubmission[]
+  serviceRequests           ServiceRequest[]
+  clubs                     Club[]
+  clubMemberships           ClubMembership[]
+  clubEvents                ClubEvent[]
+  clubEventRegistrations    ClubEventRegistration[]
+  studentSkills             StudentSkill[]
   studentPreviousEducations StudentPreviousEducation[]
-  studentInternships     StudentInternship[]
-  studentProjects        StudentProject[]
-  studentAchievements    StudentAchievement[]
-  studentLanguages       StudentLanguage[]
-  auditLogs              AuditLog[]
-  studentFeePlans        StudentFeePlan[]
-  studentTerms           StudentTerm[]
-  buildings              Building[]
-  rooms                  Room[]
+  studentInternships        StudentInternship[]
+  studentProjects           StudentProject[]
+  studentAchievements       StudentAchievement[]
+  studentLanguages          StudentLanguage[]
+  auditLogs                 AuditLog[]
+  studentFeePlans           StudentFeePlan[]
+  studentTerms              StudentTerm[]
+  buildings                 Building[]
+  rooms                     Room[]
 
   @@map("institutions")
 }
@@ -614,33 +614,33 @@ model Student {
   institution Institution @relation(fields: [institutionId], references: [id])
   user        User        @relation(fields: [userId], references: [id])
 
-  enrollments            Enrollment[]
-  attendanceRecords      AttendanceRecord[]
-  assignmentSubmissions  AssignmentSubmission[]
-  marks                  Mark[]
-  certificateRequests    CertificateRequest[]
-  grievances             Grievance[]
-  certificates           Certificate[]
-  studentDocuments       StudentDocument[]
-  feedbackSubmissions    FeedbackSubmission[]
-  serviceRequests        ServiceRequest[]
-  clubMemberships        ClubMembership[]
-  clubEventRegistrations ClubEventRegistration[]
-  studentSkills          StudentSkill[]
+  enrollments               Enrollment[]
+  attendanceRecords         AttendanceRecord[]
+  assignmentSubmissions     AssignmentSubmission[]
+  marks                     Mark[]
+  certificateRequests       CertificateRequest[]
+  grievances                Grievance[]
+  certificates              Certificate[]
+  studentDocuments          StudentDocument[]
+  feedbackSubmissions       FeedbackSubmission[]
+  serviceRequests           ServiceRequest[]
+  clubMemberships           ClubMembership[]
+  clubEventRegistrations    ClubEventRegistration[]
+  studentSkills             StudentSkill[]
   studentPreviousEducations StudentPreviousEducation[]
-  studentInternships     StudentInternship[]
-  studentProjects        StudentProject[]
-  studentAchievements    StudentAchievement[]
-  studentLanguages       StudentLanguage[]
-  studentSocialProfiles  StudentSocialProfile[]
-  feePlans               StudentFeePlan[]
-  studentTerms           StudentTerm[]
-  program                Program?                @relation(fields: [programId], references: [id])
-  programId              String?                 @db.Uuid
-  section                Section?                @relation(fields: [sectionId], references: [id])
-  sectionId              String?                 @db.Uuid
-  curriculum             Curriculum?             @relation(fields: [curriculumId], references: [id])
-  curriculumId           String?                 @map("curriculum_id") @db.Uuid
+  studentInternships        StudentInternship[]
+  studentProjects           StudentProject[]
+  studentAchievements       StudentAchievement[]
+  studentLanguages          StudentLanguage[]
+  studentSocialProfiles     StudentSocialProfile[]
+  feePlans                  StudentFeePlan[]
+  studentTerms              StudentTerm[]
+  program                   Program?                   @relation(fields: [programId], references: [id])
+  programId                 String?                    @db.Uuid
+  section                   Section?                   @relation(fields: [sectionId], references: [id])
+  sectionId                 String?                    @db.Uuid
+  curriculum                Curriculum?                @relation(fields: [curriculumId], references: [id])
+  curriculumId              String?                    @map("curriculum_id") @db.Uuid
 
   @@unique([institutionId, studentCode])
   @@unique([institutionId, admissionNumber])
@@ -820,6 +820,7 @@ model Enrollment {
   @@index([institutionId, studentId])
   @@index([institutionId, courseId])
   @@index([studentId, termId])
+  @@index([institutionId, sectionId, status])
   @@map("enrollments")
 }
 
@@ -965,6 +966,7 @@ model AttendanceRecord {
   @@unique([attendanceSessionId, studentId])
   @@index([institutionId])
   @@index([institutionId, studentId])
+  @@index([institutionId, status])
   @@index([studentId, attendanceSessionId])
   @@map("attendance_records")
 }
@@ -1553,15 +1555,15 @@ model AuditLog {
 // ─── Curriculum Models ──────────────────────────────────
 
 model Curriculum {
-  id               String           @id @default(uuid()) @db.Uuid
-  institutionId    String           @map("institution_id") @db.Uuid
-  programId        String           @map("program_id") @db.Uuid
-  versionNumber    String           @map("version_number")
-  name             String
-  status           CurriculumStatus @default(DRAFT)
-  effectiveFrom    DateTime         @map("effective_from") @db.Date
-  createdAt        DateTime         @default(now()) @map("created_at")
-  updatedAt        DateTime         @updatedAt @map("updated_at")
+  id            String           @id @default(uuid()) @db.Uuid
+  institutionId String           @map("institution_id") @db.Uuid
+  programId     String           @map("program_id") @db.Uuid
+  versionNumber String           @map("version_number")
+  name          String
+  status        CurriculumStatus @default(DRAFT)
+  effectiveFrom DateTime         @map("effective_from") @db.Date
+  createdAt     DateTime         @default(now()) @map("created_at")
+  updatedAt     DateTime         @updatedAt @map("updated_at")
 
   institution     Institution      @relation(fields: [institutionId], references: [id])
   program         Program          @relation(fields: [programId], references: [id])
@@ -1584,8 +1586,8 @@ model CurriculumTerm {
   createdAt         DateTime @default(now()) @map("created_at")
   updatedAt         DateTime @updatedAt @map("updated_at")
 
-  institution       Institution        @relation(fields: [institutionId], references: [id])
-  curriculum        Curriculum         @relation(fields: [curriculumId], references: [id], onDelete: Cascade)
+  institution       Institution               @relation(fields: [institutionId], references: [id])
+  curriculum        Curriculum                @relation(fields: [curriculumId], references: [id], onDelete: Cascade)
   curriculumCourses CurriculumCourse[]
   electiveGroups    CurriculumElectiveGroup[]
   studentTerms      StudentTerm[]
@@ -1596,7 +1598,6 @@ model CurriculumTerm {
   @@map("curriculum_terms")
 }
 
-
 model CurriculumElectiveGroup {
   id               String   @id @default(uuid()) @db.Uuid
   institutionId    String   @map("institution_id") @db.Uuid
@@ -1607,9 +1608,9 @@ model CurriculumElectiveGroup {
   createdAt        DateTime @default(now()) @map("created_at")
   updatedAt        DateTime @updatedAt @map("updated_at")
 
-  institution      Institution      @relation(fields: [institutionId], references: [id])
-  curriculumTerm   CurriculumTerm   @relation(fields: [curriculumTermId], references: [id], onDelete: Cascade)
-  courses          CurriculumCourse[]
+  institution    Institution        @relation(fields: [institutionId], references: [id])
+  curriculumTerm CurriculumTerm     @relation(fields: [curriculumTermId], references: [id], onDelete: Cascade)
+  courses        CurriculumCourse[]
 
   @@unique([curriculumTermId, name])
   @@index([institutionId])
@@ -1617,18 +1618,18 @@ model CurriculumElectiveGroup {
 }
 
 model CurriculumCourse {
-  id               String   @id @default(uuid()) @db.Uuid
-  institutionId    String   @map("institution_id") @db.Uuid
-  curriculumTermId String   @map("curriculum_term_id") @db.Uuid
-  courseId         String   @map("course_id") @db.Uuid
+  id               String                   @id @default(uuid()) @db.Uuid
+  institutionId    String                   @map("institution_id") @db.Uuid
+  curriculumTermId String                   @map("curriculum_term_id") @db.Uuid
+  courseId         String                   @map("course_id") @db.Uuid
   sequence         Int
-  creditValue      Float?   @map("credit_value")
-  isMandatory      Boolean  @default(true) @map("is_mandatory")
-  electiveGroupId  String?  @map("elective_group_id") @db.Uuid
+  creditValue      Float?                   @map("credit_value")
+  isMandatory      Boolean                  @default(true) @map("is_mandatory")
+  electiveGroupId  String?                  @map("elective_group_id") @db.Uuid
   electiveGroup    CurriculumElectiveGroup? @relation(fields: [electiveGroupId], references: [id], onDelete: SetNull)
 
-  createdAt        DateTime @default(now()) @map("created_at")
-  updatedAt        DateTime @updatedAt @map("updated_at")
+  createdAt DateTime @default(now()) @map("created_at")
+  updatedAt DateTime @updatedAt @map("updated_at")
 
   institution    Institution    @relation(fields: [institutionId], references: [id])
   curriculumTerm CurriculumTerm @relation(fields: [curriculumTermId], references: [id], onDelete: Cascade)
